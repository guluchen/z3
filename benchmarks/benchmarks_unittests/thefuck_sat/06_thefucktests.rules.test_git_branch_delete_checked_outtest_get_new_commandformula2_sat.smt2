(set-logic ALL)
(declare-const script String)
(declare-const new_command String)
(declare-const output String)
(assert (not (< (str.len script) 0)))
(assert (not (= (+ 0 (str.indexof script "\\ " 0)) (- 1))))
(check-sat)
(get-value (script))
(get-value (new_command))
(get-value (output))
